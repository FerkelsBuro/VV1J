stages:
  - build
  - execute


build:
  image: java:latest
  stage: build
  script: cd src/main/java/Domain && javac Services/*.java
  artifacts:
    untracked: true

execute:
  image: java:latest
  stage: execute
  script: cd src/main/java/Domain && java Services/WatchedFile.java
